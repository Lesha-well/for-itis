// RAM[4] = (RAM[0] * 3 + (RAM[1] | RAM[2])) & !RAM[3] + 11

// RAM[0] * 3 + (RAM[1] | RAM[2])
@1
D = M
@2
D = D | M
@0
D = D + M
D = D + M
D = D + M

// Чтобы продолжить сохранять значение в D, без записи в другие регистры,
// применим деморган к ( D & !RAM[3] )
// D & !RAM[3] = ! ( !D | RAM[3] )
D = !D
@3
D = D | M
D = !D

// + 11
@11
D = D + A

@4
M = D
